import React, { Component } from 'react';
import Panel from '../../Components/Panel';
class Saved extends Component {
  state = {

  };

  render() {
    return (
      <div className="container-fluid">
        <Panel title="Saved">
          <div>inside saved</div>
        </Panel>
      </div>
    );
  }
}

export default Saved;